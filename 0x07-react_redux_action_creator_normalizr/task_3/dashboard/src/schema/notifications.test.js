import { normalizedData, getAllNotificationsByUser } from './notifications';

describe('Normalized Data Tests', () => {
  // Test cases as shown in previous example
  // Include all the test cases from the step 3 implementation
});

describe('getAllNotificationsByUser', () => {
  // Test cases for the filter function
});
